name: black_death
description: a flutter game based on climate change.
version: 0.0.1

environment:
  sdk: '>3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  charts_flutter: ^0.12.0

flutter:
  uses-material-design: true