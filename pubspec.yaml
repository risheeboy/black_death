name: black_death
description: a flutter game based on climate change.
version: 0.0.1

environment:
  sdk: '>3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  fl_chart: ^0.65.0
flutter:
  uses-material-design: true